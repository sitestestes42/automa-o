/// <reference types="next" />
/// <reference types="next/image-types/global" />

// NOTE: este arquivo normalmente é gerado automaticamente pelo `next dev`/`next build`.
// Ele está aqui para que o projeto tenha tipos válidos mesmo antes da primeira execução.
