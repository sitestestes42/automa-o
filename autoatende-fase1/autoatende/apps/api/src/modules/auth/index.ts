/**
 * Módulo de Autenticação
 * Implementado na Fase 2.
 *
 * Responsabilidades futuras:
 * - POST /api/auth/login
 * - POST /api/auth/refresh
 * - hash de senha com bcrypt
 * - emissão de JWT (via @fastify/jwt, já registrado em src/app.ts)
 */
export {};
